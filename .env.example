# Public base URL of the deployed site. Used for metadata, sitemap, robots,
# and JSON-LD. Production value: https://www.aaazen.ca
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase project that stores leads (aaa_zen schema, leads table).
# The publishable key is safe for browsers; writes only go through the
# validated submit_aaa_zen_lead RPC.
NEXT_PUBLIC_SUPABASE_URL=https://ogjyxaawfmqzlrnuxnaa.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxx
